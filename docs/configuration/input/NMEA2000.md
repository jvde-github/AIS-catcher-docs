# Input over NMEA2000

<div class="command-container">
      <div class="command-syntax">
        <span class="cmd-name">AIS-catcher</span>
        <span class="cmd-flag">-i</span>
        <span class="cmd-value">interface</span>
        [<span class="cmd-flag">-gn</span> <span class="cmd-setting">setting</span> <span class="cmd-value">value</span>]
        ...
    </div>
</div>

AIS-catcher can read AIS messages from an NMEA2000 socketCAN interface (Linux only). NMEA2000 support must be compiled in (`-DNMEA2000=ON`).

```bash
AIS-catcher -i can0
```

## Summary Settings

<div class="input-table" markdown>

| Setting (JSON key / CLI setting name) | Type | Default | Description |
|---------|------|---------|-------------|
| Specific Options | | | |
| <span class="cmd-setting">interface</span> | string | <span class="cmd-value">-</span> | socketCAN interface name (e.g. `can0`) |

</div>
